# RunFabric config — generated by runfabric init
# Provider: vercel — set provider token; supports http, cron
service: runfabric-vercel-http-state-local

provider:
  name: vercel
  runtime: nodejs20.x
  region: ${env:AWS_REGION,us-east-1}

functions:
  handler:
    handler: dist/handler.handler
    memory: 128
    timeout: 10
    events:
      - http:
          path: /hello
          method: get
