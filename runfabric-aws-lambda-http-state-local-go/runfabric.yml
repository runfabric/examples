# RunFabric config — generated by runfabric init
# Provider: aws-lambda — set AWS_REGION; optional backend: s3 + DynamoDB for state
service: runfabric-aws-lambda-http-state-local-go

provider:
  name: aws-lambda
  runtime: go1.x
  region: ${env:AWS_REGION,us-east-1}

functions:
  handler:
    handler: handler
    memory: 128
    timeout: 10
    events:
      - http:
          path: /hello
          method: get
