{
  "name": "runfabric-aws-lambda-http-expressjs-state-local",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "doctor": "runfabric doctor",
    "plan": "runfabric plan",
    "build": "tsc -p tsconfig.json",
    "deploy": "runfabric deploy",
    "call:local": "runfabric call-local -c runfabric.yml --serve --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@runfabric/core": "^0.1.0",
    "@runfabric/provider-aws-lambda": "^0.1.0",
    "@runfabric/runtime-node": "^0.1.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
