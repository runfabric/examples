# hello-api

Generated by `runfabric init` (api, aws-lambda, ts).

## Handler Import

Use this in your handler file:

```ts
import type { UniversalHandler, UniversalRequest, UniversalResponse } from "@runfabric/core";
```

## Commands

From this project directory:

```bash
npm run doctor
npm run plan
npm run build
npm run deploy
npm run call:local
```

## Local Call (Provider-mimic)

Use built-in `runfabric call-local` to start a local HTTP server that forwards provider-shaped requests to your handler.

```bash
npm run call:local
curl -i http://127.0.0.1:8787/hello
# stop server: Ctrl+C or type 'exit' and press Enter
npm run call:local -- --provider aws-lambda --host 127.0.0.1 --port 8787 --serve --watch
npm run call:local -- --provider aws-lambda --method GET --path /hello
npm run call:local -- --provider aws-lambda --event ./event.json
```

Supported options for `call:local`:

- `--serve`
- `--watch`
- `--host <host>`
- `--port <number>`
- `--provider <id>`
- `--method <HTTP_METHOD>`
- `--path </route>`
- `--query key=value&key2=value2`
- `--header key:value` (repeatable)
- `--body <string>`
- `--event <path-to-json>`

## Credentials

Set credentials for `aws-lambda` in your shell before running deploy:

```bash
export AWS_ACCESS_KEY_ID="your-value"
export AWS_SECRET_ACCESS_KEY="your-value"
export AWS_REGION="us-east-1"
```

For real AWS deploy with internal SDK path, also set:

```bash
export RUNFABRIC_AWS_REAL_DEPLOY=1
export RUNFABRIC_AWS_LAMBDA_ROLE_ARN='arn:aws:iam::<account-id>:role/runfabric-lambda-exec'
```

If you don't have the IAM role yet, create it once:

```bash
aws iam create-role \
  --role-name runfabric-lambda-exec \
  --assume-role-policy-document '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Service":"lambda.amazonaws.com"},"Action":"sts:AssumeRole"}]}'

aws iam attach-role-policy \
  --role-name runfabric-lambda-exec \
  --policy-arn arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole

aws iam get-role --role-name runfabric-lambda-exec --query 'Role.Arn' --output text
```

Or create `.env` in this project root from `.env.example`:

```bash
cp .env.example .env
# edit values

set -a
source .env
set +a
npm run deploy
```

## Generated Files

- `runfabric.yml`
- `src/index.ts`
- `package.json`
- `.gitignore`
- `tsconfig.json`
