service: hello-api
runtime: nodejs
entry: src/index.ts

providers:
  - aws-lambda

state:
  backend: local
  local:
    dir: ./.runfabric/state

triggers:
  - type: http
    method: GET
    path: /hello

extensions:
  aws-lambda:
    region: ap-south-1
    roleArn: ${env:ROLE_ARN,''}
